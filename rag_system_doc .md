# 🧠 ระบบ AI ตอบคำถามจากเอกสารองค์กร (Local RAG + Web UI + Docker)

> **วัตถุประสงค์:**
> ให้ผู้ใช้ในองค์กรสามารถอัปโหลดเอกสาร (PDF, TXT) และถามคำถามได้ โดย AI จะตอบตามเนื้อหาเอกสารอย่างแม่นยำ
> ใช้งานได้ในเครื่อง (Offline / Local) แบบ Production-ready

---

## ⚙️ ส่วนประกอบหลักของระบบ

| ส่วน                             | เครื่องมือ / เทคโนโลยี                                   | หน้าที่                          |
| -------------------------------- | -------------------------------------------------------- | -------------------------------- |
| 🧩 **LLM Model**                 | [Ollama](https://ollama.ai) + `llama3` หรือ `mistral`    | ประมวลผลคำถามและให้คำตอบ         |
| 📚 **Vector Database**           | `pgvector` (PostgreSQL + Extension)                      | เก็บ Embedding ของเนื้อหาเอกสาร  |
| 🧠 **Embeddings Engine**         | `Instructor-XL` (ผ่าน HuggingFace Sentence Transformers) | แปลงข้อความเอกสารเป็นเวกเตอร์    |
| 🗃️ **File Parser**              | `unstructured` หรือ `PyMuPDF`                            | แปลง PDF / TXT เป็นข้อความ       |
| 🐍 **Backend API**               | FastAPI (Python)                                         | ทำหน้าที่เป็น API หลักให้ Web UI |
| 💾 **Database เก็บประวัติคำถาม** | PostgreSQL (schema แยกจาก pgvector)                      | บันทึกประวัติ Q&A ของผู้ใช้      |
| 🌐 **Frontend Web UI**           | React + TailwindCSS                                      | หน้าสำหรับอัปโหลดไฟล์และถามคำถาม |
| 🐳 **Docker Compose**            | -                                                        | จัดการทุก container และ network  |
| 🧱 **Storage Volume**            | Docker Volume                                            | เก็บเอกสารที่อัปโหลด             |

---

## 🧩 โครงสร้างไฟล์ (Project Structure)

```
ai-rag-system/
│
├── backend/
│   ├── main.py              # FastAPI หลัก
│   ├── rag_engine.py        # RAG Logic (embedding + search)
│   ├── db.py                # Connect PostgreSQL/pgvector
│   ├── requirements.txt
│
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── UploadDocument.js
│   │   │   ├── ChatInterface.js
│   │   │   └── HistoryList.js
│   │   ├── App.js
│   │   └── api.js
│
├── docker-compose.yml
└── README.md
```

---

## 🧱 docker-compose.yml (โครงสร้างระบบ)

```yaml
version: '3.8'
services:
  postgres:
    image: ankane/pgvector
    container_name: pgvector-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: ragdb
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: always

  backend:
    build: ./backend
    container_name: rag-backend
    depends_on:
      - postgres
      - ollama
    environment:
      DATABASE_URL: postgresql://admin:admin123@postgres:5432/ragdb
      OLLAMA_URL: http://ollama:11434
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    container_name: rag-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  pgdata:
  ollama_data:
```

---

## 🧠 การทำงาน (System Flow)

```mermaid
graph TD

A[ผู้ใช้] -->|อัปโหลด PDF/TXT| B[Frontend React]
B -->|ส่งไป API| C[FastAPI Backend]
C -->|แปลงไฟล์เป็นข้อความ| D[File Parser (unstructured/PyMuPDF)]
D -->|สร้าง Embedding| E[Instructor-XL]
E -->|บันทึกลงฐาน| F[pgvector DB]

A -->|ถามคำถาม| B
B -->|ส่งไป API| C
C -->|ค้นหาบริบทจาก Embedding| F
F -->|ส่ง context ที่เกี่ยวข้อง| C
C -->|ถาม Ollama LLM| G[Ollama (Llama3)]
G -->|ตอบกลับ| B
B -->|แสดงผล| A

C -->|บันทึกคำถาม-คำตอบ| H[PostgreSQL History]
```

---

## 🧰 ขั้นตอนติดตั้ง

1. **ติดตั้ง Docker และ Docker Compose**

   ```bash
   sudo apt install docker docker-compose -y
   ```

2. **Clone โครงการ**

   ```bash
   git clone https://github.com/your-org/ai-rag-system.git
   cd ai-rag-system
   ```

3. **สร้างไฟล์ `.env` (ถ้ามี)**
   เช่น Database URL, Ollama Model

4. **รันระบบทั้งหมด**

   ```bash
   docker compose up -d --build
   ```

5. **เข้าใช้งาน**

   * 🌐 Frontend: [http://localhost:3000](http://localhost:3000)
   * ⚙️ API Backend: [http://localhost:8000/docs](http://localhost:8000/docs)
   * 🧠 Ollama Model: [http://localhost:11434](http://localhost:11434)

---

## 💾 ฟีเจอร์ในระบบ

* ✅ อัปโหลดเอกสาร `.pdf`, `.txt`
* ✅ ค้นหาข้อมูลตามบริบท (RAG)
* ✅ เก็บประวัติคำถาม-คำตอบใน DB
* ✅ รองรับภาษาไทย
* ✅ ใช้งาน Local ไม่ต้องต่อเน็ต
* ✅ สามารถอัปเดตเอกสารใหม่ใน UI ได้ทันที

---

## 🚀 แนะนำโมเดลที่ใช้

| ส่วน      | Model                  | หมายเหตุ                  |
| --------- | ---------------------- | ------------------------- |
| LLM       | `llama3` (7B หรือ 8B)  | ความเข้าใจดี, ตอบธรรมชาติ |
| Embedding | `hkunlp/instructor-xl` | Semantic ดีสุด            |
| Parser    | `unstructured`         | รองรับหลายฟอร์แมต         |
| Database  | `pgvector`             | ใช้งาน RAG ได้ดีที่สุด    |

---

## 💡 ขยายเพิ่มเติม (Optional)

* 🔍 เพิ่ม Langfuse / Prometheus เพื่อตรวจสอบ performance
* 📈 เพิ่ม Streamlit dashboard สำหรับ admin monitor
* 🔐 ระบบ login สำหรับผู้ใช้แต่ละคน

---

## 📝 หมายเหตุ

ระบบนี้ออกแบบมาเพื่อใช้งานภายในองค์กรแบบ Local ทั้งหมด ไม่ต้องพึ่งพา Cloud API ภายนอก ทำให้ข้อมูลปลอดภัยและควบคุมได้เต็มที่

สำหรับการพัฒนาต่อ สามารถเพิ่ม endpoint `/upload`, `/ask`, `/history` และฟีเจอร์อื่นๆ ตามความต้องการของงานได้